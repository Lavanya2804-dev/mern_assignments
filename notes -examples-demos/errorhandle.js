//what is error
//what is issue with error?
//how to handle error?

let a=10;
console.log("a is",a)
try{
    console.log(x)
}catch(err){
    console.log(err.name)
    console.log(err.message);
    //console.log(err.stack)

}finally{
    
}

let b=20;
console.log("b is",b)
console.log("end of program")
// error identified in the compilation the error can be corrected.
// error identified in the time of execution we will handle it.
/*error occured it creates the error object and error the error object...if the that 
error obj is handled by the ourself it is good . and if that error is handled by the
java(jvm) it just explains the reason for error and terminates program*/ 
