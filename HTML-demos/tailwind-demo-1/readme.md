steps:
Inatall "tailwind caa intellisense" from extensions
create a new folder and "install the npm init -y"
and also install the  "npm install tailwindcss @tailwindcss/cli"
create a file of style.css and import tailwind
run the watch command " npx @tailwindcss/cli -i ./style.css -o ./output.css --watch"

