npm init -y---------------initializes the Node.js project with package.json
npm i -g nodemon-----------automatically restarts the server on file changes
npm i express---------creates and manages the web server and API routes
npm i mongoose------connects Node.js to MongoDB and handles database operations
npm i bcryptjs------securely hashes and compares passwords
npm i cookie-parser-----reads and parses cookies from client requests

